---
title: Radial Gradient
category: Background Effects
description: Smooth transition between radial gradients with customizable colors and positions.
---
import { BitPlayground } from '@components/BitPlayground';
import { RadialGradient as RadialGradientBit } from '@/bits';

Smooth transition between radial gradients with customizable colors and positions.

**Tags:** {RadialGradientBit.metadata.tags.join(', ')}

<BitPlayground client:only="react" bitName="RadialGradient" />
